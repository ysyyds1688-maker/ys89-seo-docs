# 娛樂城論壇自動發文腳本 — 開發規格書

## 任務目標

在 VPS 上建立一套 Python 自動化腳本，可以：
1. 爬取指定來源的文章/資訊
2. 用 Claude API 改寫成原創文章
3. 透過 WP-CLI 自動發布到 WordPress
4. 設定 SEO 欄位（Focus Keyword、Meta Description、分類）

---

## 伺服器資訊

- **VPS**：Cloudways，SSH `master_nncjxcxymn@152.42.250.98`
- **WordPress 路徑**：`/home/1561033.cloudwaysapps.com/vyjtrkxqpm/public_html/`
- **WP-CLI 可用**：`wp` 指令在路徑內直接執行（加 `--allow-root`）
- **Python**：VPS 上已有 Python3

---

## 腳本架構

```
auto_publish/
├── config.py          # API Key、設定、來源清單
├── crawler.py         # 爬蟲模組（抓來源文章）
├── rewriter.py        # Claude API 改寫模組
├── publisher.py       # WP-CLI 發佈模組
├── main.py            # 主程式（串起所有流程）
├── logs/              # 執行日誌
└── published.json     # 已發布清單（避免重複）
```

---

## 模組規格

### 1. crawler.py — 爬蟲模組

爬取來源（按優先順序）：

| 來源 | 類型 | 方式 |
|------|------|------|
| rg8888.org | 競爭對手 | requests + BeautifulSoup，爬文章列表 + 內文 |
| wager.tw | 競爭對手 | 同上 |
| players.tw | 評測站 | 同上 |
| Google News | 新聞 | `https://news.google.com/rss/search?q=娛樂城&hl=zh-TW` RSS |

**函式**：
```python
def get_articles_from_site(url, limit=5) -> list[dict]
# 回傳：[{title, content, url, source}]

def get_google_news(keyword, limit=5) -> list[dict]
# 回傳：[{title, description, url, published_date}]
```

**注意**：
- 尊重 robots.txt
- 每次請求間隔 2-3 秒（避免被封）
- 儲存原始內容到 `raw_cache/` 供 debug

---

### 2. rewriter.py — Claude API 改寫模組

**使用 Anthropic Python SDK**：
```bash
pip install anthropic
```

**改寫提示詞（Prompt）**：

```python
REWRITE_PROMPT = """
你是台灣娛樂城評測論壇的資深編輯。
請根據以下參考文章，改寫成一篇全新的繁體中文評測/分析文章。

規則：
- 字數 800-1200 字
- 使用繁體中文、台灣用語
- 不可直接抄襲，要換句話說、重新組織
- 加入自己的觀點和評論
- 結構：H2 標題分段，每段 150-200 字
- 最後加入一個「常見問題 FAQ」區塊（3-5 題）
- 語氣：中立客觀，幫玩家著想，不要太廣告感
- 輸出格式：WordPress Gutenberg Block HTML

參考文章標題：{title}
參考文章內容：
{content}

目標關鍵字（請自然融入文章）：{keywords}

請直接輸出文章 HTML，不要加任何說明。
"""
```

**函式**：
```python
def rewrite_article(title: str, content: str, keywords: list[str]) -> dict:
# 回傳：{post_title, post_content, meta_description, focus_keyword}
```

---

### 3. publisher.py — WP-CLI 發佈模組

透過 SSH 執行 WP-CLI 指令。

**函式**：
```python
def publish_post(
    title: str,
    content: str, 
    category_slugs: list[str],
    focus_keyword: str,
    meta_description: str,
    wp_path: str = "/home/1561033.cloudwaysapps.com/vyjtrkxqpm/public_html/"
) -> int:  # 回傳 post_id
```

**內部執行的 WP-CLI 指令**：
```bash
# 1. 建立文章
wp post create \
  --post_title="..." \
  --post_content="..." \
  --post_status="publish" \
  --post_type="post" \
  --post_author=1 \
  --allow-root

# 2. 設定分類
wp post term add {post_id} category {category_slug} --allow-root

# 3. 設定 Rank Math SEO 欄位
wp post meta update {post_id} rank_math_focus_keyword "{keyword}" --allow-root
wp post meta update {post_id} rank_math_description "{description}" --allow-root
```

---

### 4. config.py — 設定檔

```python
# Claude API
ANTHROPIC_API_KEY = "你的 API Key"  # 從環境變數讀取

# WordPress
WP_PATH = "/home/1561033.cloudwaysapps.com/vyjtrkxqpm/public_html/"
WP_AUTHOR_ID = 1

# SSH（用於執行 WP-CLI）
SSH_HOST = "152.42.250.98"
SSH_USER = "master_nncjxcxymn"

# 爬蟲來源
CRAWL_SOURCES = [
    "https://rg8888.org/",
    "https://wager.tw/page/casino",
]

# 發佈頻率（cron 用）
DAILY_POST_COUNT = 3  # 每天最多發幾篇
```

---

### 5. main.py — 主程式流程

```python
def main():
    # 1. 從各來源抓文章
    raw_articles = crawler.fetch_all()
    
    # 2. 過濾已發布的（比對 published.json）
    new_articles = filter_unpublished(raw_articles)
    
    # 3. 每篇用 Claude 改寫
    for article in new_articles[:DAILY_POST_COUNT]:
        rewritten = rewriter.rewrite_article(...)
        
        # 4. 發布到 WordPress
        post_id = publisher.publish_post(...)
        
        # 5. 記錄到 published.json
        mark_as_published(article['url'], post_id)
        
        # 6. 等待間隔（避免 API rate limit）
        time.sleep(30)
```

---

## 部署方式

### 安裝位置
```
/home/master_nncjxcxymn/auto_publish/
```

### 安裝步驟
```bash
# 1. 在 VPS 建立目錄
mkdir -p ~/auto_publish && cd ~/auto_publish

# 2. 安裝依賴
pip3 install anthropic requests beautifulsoup4 paramiko python-dotenv

# 3. 建立 .env 檔案（儲存 API Key）
echo "ANTHROPIC_API_KEY=你的key" > .env

# 4. 測試執行
python3 main.py --dry-run  # 只改寫不發布，看輸出
python3 main.py            # 正式發布
```

### Cron Job 設定（每天早上 8 點自動跑）
```bash
crontab -e
# 加入：
0 8 * * * cd /home/master_nncjxcxymn/auto_publish && python3 main.py >> logs/cron.log 2>&1
```

---

## 安全注意事項

1. **API Key 存在 `.env`**，不要硬寫在程式碼
2. **已發布清單** `published.json` 防止同一來源文章重複發布
3. **每篇文章改寫完先存到 `drafts/`**，確認沒問題再發布（可加 `--draft` 模式）
4. **爬蟲加 User-Agent**，模擬正常瀏覽器

---

## 完成標準

請確認以下功能都能正常運作：

- [ ] `python3 main.py --dry-run` 可輸出改寫後的文章預覽（不發布）
- [ ] `python3 main.py --single "https://目標文章網址"` 可對單篇文章跑完整流程
- [ ] `python3 main.py` 跑完後在 ys89.fun 後台看到新文章已發布
- [ ] 文章有正確的分類、Focus Keyword、Meta Description
- [ ] `published.json` 有記錄，重複跑不會重複發布
- [ ] Cron job 設定完成

---

## 需要準備的東西

**開始前請向用戶確認：**
1. Claude API Key（從 console.anthropic.com 取得）
2. 確認 SSH 連線正常：`ssh master_nncjxcxymn@152.42.250.98`

**你可以自己決定的：**
- 爬蟲細節（CSS selector、分頁邏輯）
- 錯誤處理方式
- Log 格式

---

*規格版本：v1.0 | 2026-05-06*
